create database `rafael`
# a linha a cima serve para criar um banco de dados com o nome "rafael"
# o uso da crase se torna necessárui para indivar uma `string` do sistema
# `strings` do sistema: `database`, `table` e `campo`